user_input = input('Enter some string: ')

# Using strings' built-in method
normalized = user_input.capitalize()
print(normalized)

# Using strings' built-in method
# normalized = user_input.title()
# print(normalized)

# Using slicing
# normalized = user_input[0].upper() + user_input[1:].lower()
# print(normalized)